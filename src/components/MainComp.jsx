function MainComp({ children }) {
    return (
        <main className="flex flex-wrap md:pt-10 md:max-w-[1200px] md:mx-auto">
            {children}
        </main>
    );
}

export default MainComp;
